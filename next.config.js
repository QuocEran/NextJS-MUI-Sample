/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  swcMinify: true,
  images: {
    // domains: ["dms-dlcn.s3.ap-southeast-1.amazonaws.com"],
  },
};

module.exports = nextConfig;
